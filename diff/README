<2021-04-30 Fri>

+ hide vacant tags : Do not draw tags if not used. Makes sense.
    * Skip last hunk : keep client indicator for different tags.
    - [ ] TODO shiftview skips empty.
+ colorbar:         Manage colors in different bar sections.

git init
--------

+ pertag:    Keeps layout, mwfact, barpos and nmaster per tag.
+ movestack: Move around client in focus and swap master.
+ shiftview: Move between previous/next tag.
+ center:    Add an iscentered rule to automatically center clients on the current monitor.
+ noborder:  Remove the border when there is only one window visible.

unmerged
--------

xresources: Handle any settings from Xresources. Use xrdb instead -- read colors only.
